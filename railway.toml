[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'cd /app/backend && gunicorn -c ../gunicorn.conf.py \"app:create_app()\"'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
