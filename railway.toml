[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'gunicorn -c gunicorn.conf.py \"backend.app:create_app()\"'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
